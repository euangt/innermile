<div class="container">
  <h1>All the businesses near you!</h1>
</div>

<div class="container px-10">
  <div class="cards">
    <% @businesses.each do |business| %>
      <div class="card-business">
        <p><%= link_to business.name, business_path(business) %></p>
        <p><%= business.short_bio %></p>
      </div>
    <% end %>
  </div>
</div>